spec:
  template:
    spec:
      containers:
      - name: image-clf-inf
        resources:
          requests: 
            cpu: 8
          limits:
            cpu: 8
        volumeMounts:
        - mountPath: /racelab
          name: wtb-pv
      restartPolicy: Always
      volumes:
      - name: wtb-pv
        persistentVolumeClaim:
          claimName: wtb-pvc
